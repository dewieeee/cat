{
  "name": "cat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "vite build && gh-pages -d dist",
    "build": "vite build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.3.0"
  }
}
